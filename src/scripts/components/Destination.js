'use strict';

var React = require('react/addons');

require('styles/Destination.css');

var Destination = React.createClass({
  render: function () {
    return (
        <div>
          <p>Content for Destination</p>
        </div>
      );
  }
});

module.exports = Destination; 

