'use strict';

var React = require('react/addons');

require('styles/Support.css');

var Support = React.createClass({
  render: function () {
    return (
        <div>
          <p>Content for Support</p>
        </div>
      );
  }
});

module.exports = Support; 

