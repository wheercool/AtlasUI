'use strict';

var React = require('react/addons');

require('styles/TryForFree.css');

var TryForFree = React.createClass({
  render: function () {
    return (
        <div>
          <p>Content for TryForFree</p>
        </div>
      );
  }
});

module.exports = TryForFree; 

